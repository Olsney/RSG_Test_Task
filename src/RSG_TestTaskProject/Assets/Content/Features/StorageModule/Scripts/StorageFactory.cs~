namespace Content.Features.StorageModule.Scripts {
    public class StorageFactory : IStorageFactory
    {
        private const float Capacity = 10;
        
        public IStorage GetStorage() =>
            new StandardStorage(Capacity);
    }
}